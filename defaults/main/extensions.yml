# install and enable the HTTPS Everywhere browser extension
install_https_everywhere_ext: false

# install and enable the uBlock Origin browser extension
install_ublock_origin_ext: false

# install and enable the Surfingkeys browser extension
install_surfingkeys_ext: false

# install google-widevine add-on for DRM content (e.g. Netflix)
install_google_widevine_drm_addon: false

